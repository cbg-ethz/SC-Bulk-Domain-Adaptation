Currently, this repository only contains a tiny subset of one dataset for demonstration purposes. 
For replicating the benchmark, download the processed datasets from [Zenodo](https://zenodo.org/records/21843088). Then unzip the processed datasets in `processed/`.
